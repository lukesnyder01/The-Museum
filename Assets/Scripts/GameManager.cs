using UnityEngine;

public static class GameManager
{
    public static bool spawnPointSet = false;
    public static Vector3 targetPlayerPos;
    public static Vector3 targetPlayerRot;

    // An attempt to get the class to initialize properly
    static GameManager()
    { 
    
    }
}
